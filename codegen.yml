schema: http://localhost:8000/subgraphs/name/anderjaska/phlote-curator-mumbai-staging
documents: "./lib/graphql/**/*.graphql"
generates:
  ./lib/graphql/generated.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
    config:
      scalars:
        Bytes: Uint8Array
        BigInt: number
        BigDecimal: number
