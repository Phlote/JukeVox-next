query SubmissionsSearch($searchTerm: String!) {
  submissionsSearch(text: $searchTerm) {
    id
    timestamp
    artistName
    mediaTitle
    mediaType
    mediaURI
    marketplace
    cosigns
    submitterWallet
  }
}
