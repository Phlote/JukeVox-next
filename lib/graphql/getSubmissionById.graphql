query GetSubmissionById($id: ID) {
  submissions(where: { id: $id }) {
    id
    timestamp
    artistName
    mediaTitle
    mediaType
    mediaURI
    marketplace
    cosigns
    submitterWallet
  }
}
