query GetSubmissions($filter: Submission_filter, $skip: Int) {
  submissions(
    first: 10
    orderBy: timestamp
    orderDirection: desc
    where: $filter
    skip: $skip
  ) {
    ...SubmissionArchiveFields
  }
}
